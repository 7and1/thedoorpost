{
  "name": "thedoorpost",
  "private": true,
  "scripts": {
    "test": "npm --prefix apps/worker test && npm --prefix apps/web test",
    "lint": "npm --prefix apps/worker lint && npm --prefix apps/web lint"
  },
  "engines": {
    "node": ">=18"
  }
}
